name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  documentation-contracts:
    name: Documentation contracts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Validate documentation contracts
        run: python3 scripts/docs/validate_contracts.py

      - name: Test validator
        run: python3 tests/tooling/test_docs_contracts.py

  # ─────────────────────────────────────────────
  # Frontend: lint + typecheck + unit tests
  # ─────────────────────────────────────────────
  frontend:
    name: Frontend (lint · typecheck · test)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit -p tsconfig.app.json

      - name: Unit tests
        run: npm test -- --reporter=verbose

  # ─────────────────────────────────────────────
  # Backend: lint + typecheck + unit/integration tests
  # ─────────────────────────────────────────────
  backend:
    name: Backend (lint · typecheck · unit · integration)
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: neon
          POSTGRES_PASSWORD: ci_password
          POSTGRES_DB: neon_arsenal_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U neon -d neon_arsenal_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    defaults:
      run:
        working-directory: server

    env:
      DATABASE_URL: postgresql://neon:ci_password@localhost:5432/neon_arsenal_test

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Lint
        run: npm run lint

      - name: Apply Prisma migrations
        run: npm run test:db:prepare

      - name: Type check
        run: npx tsc --noEmit

      - name: Unit tests
        run: npm run test:unit -- --reporter=verbose

      - name: Integration tests (PostgreSQL)
        run: npm run test:integration -- --reporter=verbose

  # ─────────────────────────────────────────────
  # Contract: OpenAPI vs real HTTP handlers (#61)
  # ─────────────────────────────────────────────
  contract:
    name: Contract (OpenAPI)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: server

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Contract tests
        run: npm run test:contract -- --reporter=verbose

  # ─────────────────────────────────────────────
  # Security: dependency policy (SPEC-0012)
  # ─────────────────────────────────────────────
  security:
    name: Security (npm audit)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install frontend dependencies
        run: npm ci

      - name: Gate frontend runtime High/Critical
        run: npm audit --omit=dev --audit-level=high

      - name: Gate frontend toolchain Critical
        run: npm audit --audit-level=critical

      - name: Install backend dependencies
        working-directory: server
        run: npm ci

      - name: Gate backend runtime High/Critical
        working-directory: server
        run: npm audit --omit=dev --audit-level=high

      - name: Gate backend toolchain Critical
        working-directory: server
        run: npm audit --audit-level=critical

  # ─────────────────────────────────────────────
  # Security: Trivy filesystem gate (#68 / SPEC-0012)
  # No image registry in this repo; Render builds the server/ context.
  # ─────────────────────────────────────────────
  trivy:
    name: Security (Trivy High/Critical gate)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Gate server/ High/Critical findings
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: server
          severity: CRITICAL,HIGH
          exit-code: "1"

  # ─────────────────────────────────────────────
  # Build verification (frontend + backend)
  # ─────────────────────────────────────────────
  build:
    name: Build check
    runs-on: ubuntu-latest
    needs: [frontend, backend, contract, security, trivy]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build
        env:
          API_URL: https://api.example.com

      - name: Setup Node.js (backend)
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json

      - name: Install backend dependencies
        working-directory: server
        run: npm ci

      - name: Generate Prisma client
        working-directory: server
        run: npx prisma generate

      - name: Build backend
        working-directory: server
        run: npm run build
